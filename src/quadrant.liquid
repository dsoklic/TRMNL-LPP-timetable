<div class="layout layout--left layout--top">
  <div class="columns" data-overflow="true" data-overflow-max-cols="1">
    <div class="column">
      {% for group in data.route_groups %}
        {% for route in group.routes %}
          {% render "route_item" with group: group, route: route, max_minutes: 2 %}
        {% endfor %}
      {% endfor %}
    </div>
  </div>
</div>

{{ title_bar_markup }}
